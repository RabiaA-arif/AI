# my venv name is "virtual"
for activation of venc us command `virtual\scripts\activate`

*`uvicorn main:app --reload`*
uvicorn: is the server
main : is file name
app: we declare application in main.py file
--relaod:when ever we do changes so it will automatically load